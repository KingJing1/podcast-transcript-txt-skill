interface:
  display_name: "Podcast Transcript TXT"
  short_description: "Export podcast transcript TXT (draft first, then LLM-proofread)"
  default_prompt: "Use $podcast-transcript-txt to get a full transcript TXT from a link/title, and explicitly remind that output is a draft requiring one strong-LLM proofreading pass."
